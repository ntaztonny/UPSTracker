type Order {
  Address: String
  carrier: String
  createdAt: Date
  shippingCost: Int
  trackingId: String
  City: String
  Lat: Float
  Lng: Float
  #Tracking
  trackingItems: TrackingItems
    @materializer(
      query: "getTrackingItemsById"
      arguments: [{ name: "id", field: "trackingId" }]
    )
}

type OrderList {
  name: ID
  value: Order
}
type Query {
  getOrders: [OrderList]
    @rest(
      endpoint: "https://upstracker-ceacb-default-rtdb.firebaseio.com/orders.json"
      transforms: [{ pathpattern: [], editor: "objectToArray" }]
    )
}
